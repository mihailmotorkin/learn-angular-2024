import { TestFromEventDirective } from './test-from-event.directive';

describe('TestFromEventDirective', () => {
  it('should create an instance', () => {
    const directive = new TestFromEventDirective();
    expect(directive).toBeTruthy();
  });
});
